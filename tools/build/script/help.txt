
build quick
	- compila projetos

build test
	- compila e roda testes unitarios e integrados

build package
	- gera pacote de instalação/atualização

build deploy -D:enviroment=approval -D:file=\\build\packages\newecm\versao_atual\veros-ecm_update_versao_atual.exe
	- atualiza o servidor de homologacao (ecmtest) com a versao atual. Onde "approval" corresponde ao arquivo
	 "approval_deploy.config" na pasta "config" que deve conter o seguinte conteudo:

	<deploy>

		<server>192.168.10.16</server>
		<user>ecm</user>
		<password>v3r0s1@</password>
		<remote>true</remote>
	
	</deploy>

task clean
	- exclui arquivos desnecessarios como pasta bin, debug

task svnignore
	- marca arquivos a serem ignorados pelo subversion
